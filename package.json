{
  "name": "bahmni-ndhm-tests",
  "description": "To test the ndhm compliance of Bahmni",
  "scripts": {
    "ci": "gauge run --env ci --tags 'core' specs -v",
    "ndhm": "gauge run --env ndhm --tags 'core' specs -v",
    "test": "gauge run --env default --tags 'core' specs -v"
  },
  "dependencies": {
    "taiko": "latest",
    "axios": "latest",
    "buffer": "latest"
  }
}
