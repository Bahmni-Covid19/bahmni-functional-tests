{
  "name": "gauge-taiko-template",
  "description": "Starter template for writing JavaScript tests for Gauge",
  "scripts": {
    "test": "gauge run --env ci --tags 'core' specs -v"
  },
  "dependencies": {
    "taiko": "latest",
    "axios": "latest",
    "buffer": "latest"
  }
}
